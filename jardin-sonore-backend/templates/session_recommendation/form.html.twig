{% extends 'internal/base.html.twig' %}

{% set title = item ? 'sessions.recommendation.edit.title'|trans({ title: item.title }, 'sessions') : 'sessions.recommendation.new.title'|trans({}, 'sessions') %}

{% block internal_body %}
    <section class="internal-page-heading">
        <div>
            <p class="internal-eyebrow">{{ (item ? 'sessions.recommendation.edit.eyebrow' : 'sessions.recommendation.new.eyebrow')|trans({}, 'sessions') }}</p>
            <h1>{{ item ? item.title : 'sessions.recommendation.new.heading'|trans({}, 'sessions') }}</h1>
            <p>{{ (item ? 'sessions.recommendation.edit.description' : 'sessions.recommendation.new.description')|trans({}, 'sessions') }}</p>
        </div>
        <a class="internal-button internal-button--secondary" href="{{ path('session_recommendation_index') }}">{{ 'sessions.recommendation.form.back'|trans({}, 'sessions') }}</a>
    </section>
    <section class="internal-card">
        {% if hasErrors %}<div class="internal-alert internal-alert--error">{{ 'sessions.recommendation.form.error_notice'|trans({}, 'sessions') }}</div>{% endif %}
        {{ form_start(form, { attr: { class: 'internal-form' } }) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </section>
{% endblock %}
