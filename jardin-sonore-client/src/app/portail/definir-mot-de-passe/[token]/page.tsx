import PortalPasswordForm from "@/components/portal/PortalPasswordForm";
import {getTranslations} from "@/i18n/server";
export default async function PortalPasswordPage({params}: {params: Promise<{token:string}>}): Promise<React.JSX.Element> { const [{token},dictionary]=await Promise.all([params,getTranslations()]); const c=dictionary.portal.password; return <section className="portal-shell flex min-h-screen items-center justify-center px-4 py-16"><div className="w-full max-w-[28rem] rounded-xl border border-outline-variant bg-white p-8 shadow-sm"><p className="portal-eyebrow">{c.eyebrow}</p><h1 className="font-serif text-3xl font-semibold">{c.title}</h1><p className="mt-3 text-sm text-on-surface-variant">{c.description}</p><div className="mt-8"><PortalPasswordForm token={token}/></div></div></section>; }
